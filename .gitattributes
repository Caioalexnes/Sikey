# Normaliza tudo em LF no repositorio. Sem isto o gradlew sai com CRLF e nao
# roda em Linux nem macOS: o shell tenta interpretar o \r do fim da linha.
* text=auto eol=lf

*.bat text eol=crlf
gradlew text eol=lf

*.png binary
*.jar binary
*.xcf binary
*.apk binary
